# useForm

Ejemplo de uso:
```
    const initalForm = {
        name: '',
        age: 0,
        email: ''
    };

    const [formValues, habdleInputChange, reset] = useForm( initialForm );
    
```